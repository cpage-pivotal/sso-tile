spring.application.name=sso-tile

# OAuth 2.0 Client Configuration for Tanzu Single Sign-On
# These properties will be overridden by VCAP_SERVICES when bound to an SSO service instance on Cloud Foundry.
# For local development, you can configure these manually or use a local identity provider.

# Uncomment and configure for local testing:
# spring.security.oauth2.client.registration.sso.client-id=${SSO_CLIENT_ID}
# spring.security.oauth2.client.registration.sso.client-secret=${SSO_CLIENT_SECRET}
# spring.security.oauth2.client.registration.sso.scope=openid,profile,email
# spring.security.oauth2.client.registration.sso.authorization-grant-type=authorization_code
# spring.security.oauth2.client.registration.sso.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}
# spring.security.oauth2.client.provider.sso.issuer-uri=${SSO_ISSUER_URI}
